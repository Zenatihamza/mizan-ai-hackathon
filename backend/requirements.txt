fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pytesseract==0.3.13
Pillow==11.0.0
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
SQLAlchemy==2.0.36
PyJWT==2.10.1
scikit-learn==1.5.2
joblib==1.4.2
# Optional: ChromaDB for vector RAG. Requires Microsoft C++ Build Tools on Windows.
# Without it, the system uses keyword search (works fine for the demo corpus).
# To enable: install Build Tools from https://visualstudio.microsoft.com/visual-cpp-build-tools/
# then: pip install chromadb==0.5.20 sentence-transformers==3.3.1
